[tool.poetry]
name = "matrix-loader"
version = "0.1.0"
description = "Matrix data analysis tool for 42"
authors = ["Adrian <tu_correo@student.42.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pandas = "2.1.0"
numpy = "1.25.0"
matplotlib = "3.7.2"
requests = "2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"